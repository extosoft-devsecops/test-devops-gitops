app:
  replicaCount: 3
  image:
    tag: "latest" # CI จะ update ค่านี้
  env:
    nodeEnv: production
  resources:
    limits:
      cpu: 500m
      memory: 512Mi

graphite:
  persistence:
    enabled: true
    size: 10Gi
    storageClass: "gp2" # 👈 สำคัญ: ชื่อ Storage Class ของ AWS EBS

# ถ้ามี Ingress (ใช้ ALB Controller)
ingress:
  enabled: true
  className: "alb"
  annotations:
    alb.ingress.kubernetes.io/scheme: internet-facing