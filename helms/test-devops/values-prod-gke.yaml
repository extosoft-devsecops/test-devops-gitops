app:
  replicaCount: 3
  image:
    tag: "latest" # CI จะ update ค่านี้
  env:
    nodeEnv: production
  resources:
    limits:
      cpu: 500m
      memory: 512Mi

graphite:
  persistence:
    enabled: true
    size: 10Gi
    storageClass: "standard-rwo" # 👈 สำคัญ: ชื่อ Storage Class ของ GKE

# ถ้ามี Ingress
ingress:
  enabled: true
  className: "gce" # หรือ nginx
  annotations:
    kubernetes.io/ingress.class: "gce"